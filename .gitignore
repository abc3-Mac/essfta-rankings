__pycache__/
*.pyc
.DS_Store
.claude/
# internal handoff notes (Ghost page IDs / publishing workflow) — not for the public repo
HANDOFF.md
# internal update-system spec (NAS/infra/credential model) — private only
SPEC.md
# superseded first-pass widget
breed_group_widget.html
# one-off blog-post drafts (publishing artifacts)
blog_post.html
blog_post_final.html

# PRIVATE — member PII + raw AKC source files. Kept out of the PUBLIC repo.
# (build_calculator.py degrades gracefully to no membership flags when this is absent.)
raw_akc/
# the BUILT calculator embeds the 702-member directory inline → private only
rankings_calculator.html
# personal backup tooling (points at the private repo) — backed up to private, not public
backup_to_private.sh
